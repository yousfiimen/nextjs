/** @type {import('next').NextConfig} */
// next.config.mjs
export default {
    // Configurations spécifiques de Next.js vont ici.
    // Par exemple, la configuration du répertoire de sortie (distDir).
    distDir: 'out',
  };
  